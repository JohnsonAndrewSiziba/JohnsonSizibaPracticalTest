import { FilterStocksPipe } from './filter-stocks.pipe';

describe('FilterStocksPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStocksPipe();
    expect(pipe).toBeTruthy();
  });
});
